/**
 * This module exports the useStakeTokens hook.
 * The useStakeTokens hook provides functionalities for approving and staking tokens
 * within the dApp. It handles interactions with the blockchain to perform these operations
 * and manages the state associated with these actions.
 *
 * @module useStakeTokensHook
 */
export { useStakeTokens } from "./useStakeTokens"
